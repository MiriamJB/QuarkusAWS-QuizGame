import React from 'react';

const HomePage = () => {
    return (
        <div className="container">
            <h1>Home Page</h1>
            <p>Welcome to the home page. You are signed in!</p>
        </div>
    );
};

const styles = {
    container: { textAlign: 'center', marginTop: '5rem' }
};

export default HomePage;
